$(document).ready(function() {

    /* Reload if job not finished */
    if (#{jsPoll})
       setTimeout(function() {
                     document.location.reload(true);
                   }, 10*1000);
  }
);
